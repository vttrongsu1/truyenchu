{
  "name": "nghe-truyen",
  "version": "1.0.0",
  "description": "Root project for deployment",
  "scripts": {
    "install-all": "npm install --prefix backend && npm install --prefix app",
    "build": "npm run build --prefix app",
    "start": "node backend/server.js"
  }
}
